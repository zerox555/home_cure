import 'package:flutter/material.dart';

class HouseStatus extends StatelessWidget {
  const HouseStatus({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Suspicious report page"),
      ),
    );
  }
}
