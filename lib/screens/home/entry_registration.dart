import 'package:flutter/material.dart';

class EntryRegistration extends StatelessWidget {
  const EntryRegistration({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Entry registration page"),
      ),
    );
  }
}
